#include <stdlib.h>
#include <stdio.h>
#include "isMinHeap.h"

/*
    given a tree, check whether it is a minimum heap.
    note it must ensure the tree structure is maintained as well.
    this is meant as a challenge activity.
    Queue.h is provided for you if you wish to use it

    Compile:
    gcc -Wall -Werror -O -o testIsMinHeap isMinHeap.c isMinHeapTest.c [Queue.c]
*/

int isMinHeap(treelink tree) {
    return 1;
}